{
	"name": "checkoutly",
	"version": "0.7.2",
	"private": true,
	"description": "Checkoutly classic checkout builder dashboard.",
	"scripts": {
		"build:zip": "rm -rf dist && mkdir -p dist/checkoutly && rsync -a --exclude-from=.distignore ./ dist/checkoutly/ && cd dist && zip -qr checkoutly.zip checkoutly",
		"lint:php": "find . -path ./node_modules -prune -o -path ./vendor -prune -o -path ./dist -prune -o -path ./.git -prune -o -name '*.php' -print -exec php -l {} \\;",
		"lint:wpcs": "vendor/bin/phpcs",
		"test:php": "php tests/php/SanitizerTest.php && php tests/php/WorkflowTest.php && php tests/php/ClassicCheckoutTest.php && php tests/php/DisplayTest.php && php tests/php/DiagnosticsTest.php && php tests/php/RoutingTest.php",
		"test": "npm run test:php"
	}
}
